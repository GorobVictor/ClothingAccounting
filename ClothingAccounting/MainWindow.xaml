<Window x:Class="ClothingAccounting.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClothingAccounting"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <ListView x:Name="listView_Balance" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Top">
            <ListView.View>
                <GridView>
                    <!--<GridViewColumn Header="IdProduct" Width="30" DisplayMemberBinding="{Binding IdProduct}" />
                    <GridViewColumn Header="IdSizeProduct" Width="120" DisplayMemberBinding="{Binding IdSizeProduct}" />
                    <GridViewColumn Header="IdColorProduct" Width="120" DisplayMemberBinding="{Binding IdColorProduct}" />
                    <GridViewColumn Header="IdStaff" Width="120" DisplayMemberBinding="{Binding IdStaff}" />
                    <GridViewColumn Header="Quantity" Width="120" DisplayMemberBinding="{Binding Quantity}" />-->
                </GridView>
            </ListView.View>
        </ListView>
        <DataGrid x:Name="datagrid_Balance" Margin="20"/>

    </Grid>
</Window>
